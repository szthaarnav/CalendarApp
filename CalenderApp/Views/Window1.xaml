<Window x:Class="CalenderApp.Views.EntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Event Details" Height="300" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Date:"/>
            <DatePicker SelectedDate="{Binding CurrentEntry.EventDate}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Title:"/>
            <TextBox Text="{Binding CurrentEntry.Title}" Padding="2"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Description:"/>
            <TextBox Text="{Binding CurrentEntry.Description}" AcceptsReturn="True" Height="60" Padding="2"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Save" Width="100" HorizontalAlignment="Right" Click="SaveButton_Click"/>
    </Grid>
</Window>